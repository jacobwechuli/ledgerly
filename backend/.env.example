# Server
PORT=8080
ENVIRONMENT=development

# Database (required)
DATABASE_URL=postgresql://user:password@localhost:5432/ledgerly

# Redis (recommended for production rate limiting)
REDIS_URL=redis://localhost:6379

# Clerk Authentication
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key
CLERK_ISSUER_URL=https://your-app.clerk.accounts.dev

# Groq (optional, for AI features)
GROQ_API_KEY=gsk_your-groq-api-key

# Rate Limiting
RATE_LIMIT_RPM=60

# Supported Currencies (seeded in DB): USD, KES, GBP, EUR, CHF, JPY, ZAR, NGN, AED, SGD
